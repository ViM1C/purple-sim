# Rename this to secrets.py on your T-Dongle (do NOT commit secrets.py).
WIFI_SSID = "YOUR_WIFI"
WIFI_PSK  = "YOUR_PASSWORD"

# Optional for later collector use; leave empty for now.
COLLECTOR_URL = ""

# Optional HMAC key for later; can stay as-is.
SHARED_KEY = b"supersecret-shared-key-32bytes"

# Human-friendly board name
DEVICE_ID = "tdongle-s3-01"
